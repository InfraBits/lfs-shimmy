[tox]
requires = tox>=4
envlist = ruff,black,bandit

[testenv]
package = skip
use_current_env = true

[testenv:ruff]
commands = ruff check {posargs:app.py}
allowlist_externals = ruff

[testenv:black]
commands = black --check {posargs:app.py}
allowlist_externals = black

[testenv:bandit]
commands = bandit -r {posargs:app.py}
allowlist_externals = bandit
